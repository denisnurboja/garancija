<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Model</th>
      <th>Image</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<tbody>
	<tr>
  		<td><%= @product.name %></td>
		<td><%= @product.model %></td>
		<td><%= @product.image %></td>
		<td><%= @product.description %></td>
	</tr>
</tbody></table>

<%= link_to 'Edit', edit_product_path(@product), class: 'btn btn-success' %> 
<%= link_to 'Back', products_path, class: 'btn btn-success' %>
<%= link_to 'Product registrations', product_product_registrations_path(@product), class: 'btn btn-success' %>
