<%= form_for(@user, html: {class:"form-horizontal bv-form"}) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
  <legend>Registracija novog korisnika</legend>
    <div class="form-group">
    <%= f.text_field :first_name, placeholder: "Ime", class: "col-xs-3"%>
    <%= f.text_field :last_name, placeholder: "Prezime", class: "col-xs-3" %>
    </div>
    <div class="form-group">
    <%= f.text_field :email, placeholder: "Email", class: "col-xs-3" %>
    <%= f.text_field :phone, placeholder: "Telefon", class: "col-xs-3"%>
    </div>
    <div class="form-group">  
    <%= f.text_field :adress, placeholder: "Adresa", class: "col-xs-2"%>
    <%= f.text_field :pobox, placeholder: "Po.broj", class: "col-xs-2" %>  
    <%= f.text_field :city, placeholder: "Grad", class: "col-xs-2" %>
    </div>
    </fieldset>
    <%= f.submit "Spasi" %>
<% end %>
